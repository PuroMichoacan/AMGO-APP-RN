import { View } from "react-native";
const RegistroSet = () => {

    // const navigation = useNavigation();

    // navigation.setOptions({
    //   title : 'Registro set',      
      
    // })

  return <View></View>;
};

export default RegistroSet;
